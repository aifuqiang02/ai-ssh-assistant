# 重构记录

## 2026-04-07

### 本次处理范围

- 清理前端 `apps/desktop` 中高频开发期调试日志，重点是聊天、文件管理、SSH
  IPC、AI 请求链路。
- 清理后端 `packages/server` 中纯 `console.*`
  调试输出，保留正式错误日志与必要告警。

### 为什么处理

- 这些日志会在桌面端控制台和后端启动/运行时持续刷屏，影响真实问题排查。
- 部分后端错误被 `logger` 和 `console.error` 重复打印，信息冗余。

### 删掉了什么

- `apps/desktop/src/components/chat/AIChatOpenCodeStyle.vue`
  中请求生命周期、性能摘要、工具执行跟踪日志。
- `apps/desktop/src/views/FileManagerView.vue`
  中列表加载、上传下载队列、拖拽收集、任务状态更新等流程跟踪日志。
- `apps/desktop/electron/ipc/ssh-handlers.ts`
  中命令执行细粒度追踪、目录列取耗时打印、IPC
  handler 入参打印、上传下载进度调试日志。
- `apps/desktop/src/services/processor.ts`、`ai-stream.service.ts`、`ai-api.service.ts`、`llm.ts`
  中 AI 请求/工具调用调试日志。
- `apps/desktop/src/components/layout/AppSidebar.vue`
  中连接/打开文件管理器流程日志。
- `packages/server/src/index.ts` 中与 `logger` 重复的 `console.error`。
- `packages/server/src/services/ssh.service.ts` 中 SSH 测试连接调试日志。
- `packages/server/src/config/i18n.ts`、`packages/server/src/config/app.config.ts`
  中控制台调试输出。

### 重构了什么

- 将 `packages/server/src/config/app.config.ts`
  中仍有价值的生产环境告警与配置输出统一改为 `logger`。
- 将前端工具消息状态类型补齐为支持 `aborted`，与现有界面逻辑保持一致。

### 明确保留了什么以及原因

- 保留 `console.error` / `console.warn`
  这类真实故障与异常告警输出，避免清理后失去排障信息。
- 未处理构建产物、示例插件、以及其他业务文件中的次级日志，避免在本次日志清理中扩大范围到无关模块。

### 验证结果

- `pnpm --filter @ai-ssh/server type-check`：通过。
- `pnpm --filter @ai-ssh/desktop type-check`：未通过，但失败项主要为仓库既有类型问题，如
  `electron/ipc/file-handlers.ts`、`AppSidebar.vue`
  文档树相关未定义符号、`ai-api.service.ts` 的 `MessageMetadata`
  导出不匹配等；本次日志清理额外暴露的 `ToolState` 缺少 `aborted` 已修正。

### 下次可继续关注的点

- 继续收口 `apps/desktop/src/views/TerminalView.vue`、`SettingsView.vue`
  等剩余散落的调试日志。
- 单独开一轮处理桌面端既有 TypeScript 类型错误，避免后续清理/重构验证噪音过大。

## 2026-04-07（第二轮）

### 本次处理范围

- 继续清理桌面端剩余高频页面层与服务层调试日志。

### 为什么处理

- 第一轮后，终端页、设置页、提示词优化页仍有明显流程跟踪日志。
- 个别服务模块仍会把刷新诊断、聊天日志文件写入过程等细节同步输出到控制台。

### 删掉了什么

- `apps/desktop/src/views/TerminalView.vue`
  中终端输入、重连成功、快捷命令执行、远程编辑同步、模型加载等 `console.log`。
- `apps/desktop/src/views/SettingsView.vue`
  中模型测试、模型刷新、手动刷新托管模型、模型开关、打开存储目录等
  `console.log`。
- `apps/desktop/src/views/PromptOptimizerView.vue`
  中模型加载、提示词保存成功、事件触发后的状态日志。
- `apps/desktop/src/services/openrouter-free-model-refresh.service.ts`
  中 OpenRouter Free 刷新诊断日志，并删除只服务于该日志的辅助函数。
- `apps/desktop/src/services/chat-logger.ts`
  中日志文件初始化、写入、追加成功的控制台输出。

### 重构了什么

- 同步删除 `SettingsView.vue` 和 `openrouter-free-model-refresh.service.ts`
  中因日志清理而不再使用的 import / 辅助函数。

### 明确保留了什么以及原因

- 保留 `console.error` /
  `console.warn`，尤其是连接失败、复制失败、刷新失败、文件写入失败等真实异常。
- 暂未处理
  `SettingsPlugins.vue`、`example-plugins.ts`、`skills/registry.ts`、`base/service-factory.ts`
  等模块中的日志，避免把可能有意保留的开发可观测性一起删除。

### 验证结果

- `pnpm --filter @ai-ssh/desktop type-check`：未通过。
- 失败项仍以仓库既有桌面端类型问题为主，例如 `electron/ipc/file-handlers.ts` 的
  `renameFile/copyFile`、`AppSidebar.vue`
  文档树相关未定义符号、`ai-api.service.ts` 的 `MessageMetadata` 导出不匹配等。
- 本轮清理后未新增新的 `console.log/info` 残留于本次处理文件中。

### 下次可继续关注的点

- 继续盘点并清理
  `SettingsPlugins.vue`、`base/service-factory.ts`、`skills/registry.ts`、`example-plugins.ts`
  等剩余日志。
- 若要进一步验证日志清理效果，建议先单独修复桌面端现有 TypeScript 报错，再跑更完整的 lint
  / build。

## 2026-04-07（多源更新机制）

### 本次处理范围

- 为桌面端新增多源更新机制基础能力。
- 为发布流程新增平台更新清单生成与上传。

### 为什么处理

- 现有仓库没有真正的桌面端自动更新实现，也没有把更新元数据上传到 Release。
- 需要在软件启动后后台检查新版本、探测多个下载源、提前缓存安装包，并在用户确认后直接安装。

### 删掉了什么

- 无。

### 重构了什么

- 新增 `apps/desktop/scripts/generate-update-manifest.mjs`，按平台生成
  `update-manifest-<platform>-<arch>.json`。
- 更新
  `.github/workflows/release.yml`，在打包后生成并上传更新清单、`yml`、`blockmap`
  等元数据文件。
- 新增
  `apps/desktop/electron/services/update-service.ts`，封装版本比较、缓存复用、最快源选择、后台下载、安装触发。
- 补充 manifest 多源容灾 URL 组装逻辑，优先 GitHub，再回退到构建期注入的镜像 manifest 基地址。
- 补充安装包代理模板源支持，可生成
  `https://git.aifuqiang.win/{github_release_url}` 这类代理下载链接。
- 为更新主链路补充结构化日志，覆盖 manifest 拉取、源测速、缓存命中、下载完成、安装触发与失败原因。
- 在主进程、preload、Renderer 类型声明中新增 updater IPC 通道。
- 新增
  `apps/desktop/src/services/update-client.ts`，统一管理 Renderer 侧更新状态。
- 在 `App.vue` 中加入启动即后台检查更新与“下载完成后立即安装”通知。
- 在 `SettingsView.vue`
  中新增“应用更新”区块，展示更新状态并提供“重新检查更新 / 立即安装更新”按钮。
- 细化三端安装行为：Windows 直接启动安装包，macOS 打开缓存的 DMG，Linux 对
  `AppImage` 直接启动，对其他包型交给系统打开。

### 明确保留了什么以及原因

- 仍保留 GitHub Release 作为权威发布源，并固定增加 `git.aifuqiang.win`
  代理源，避免发布时再依赖额外环境变量。
- 当前 Linux 默认优先选择 `AppImage`，macOS 默认优先选择
  `dmg`，这是为了先用最少逻辑覆盖三端安装包缓存能力。

### 验证结果

- `node --test "apps/desktop/scripts/generate-update-manifest.test.mjs"`：通过。
- `pnpm exec tsx --test "apps/desktop/electron/services/update-service.test.ts"`：通过。
- `node --test "apps/desktop/src/App.test.ts" "apps/desktop/src/views/SettingsView.test.ts"`：通过。
- `pnpm --filter @ai-ssh/desktop type-check`：通过。
- 额外收敛了部分旧类型问题：`ai-api.service.ts` 的 `MessageMetadata` 导入、
  `SessionSettingsView.vue` 的 `ChatSession` 导入、`electron.d.ts`
  中 SSH/FS 接口声明漂移、`SettingsView.vue` 的 `warning` 通知类型、
  `file-handlers.ts` 缺失的文件方法、`SettingsMCP.vue` 对私有配置的直接访问、
  `mcp/index.ts` / `bash-tool.ts` 的签名不匹配，以及 `AppSidebar.vue`
  中未接线文档树残留死代码。

### 下次可继续关注的点

- 后续可以继续补 `AppSidebar.vue`
  被移除的文档树能力，或进一步拆分该文件，避免死代码再次回流。
